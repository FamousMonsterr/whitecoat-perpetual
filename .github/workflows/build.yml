name: Unity Windows Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  checks: write      # test-runner публикует чек "Test Results"
  contents: write    # release-job создаёт Release

env:
  # Принудительный переход на Node.js 24 для совместимости с GitHub Actions
  # Документация: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # Unity 6000.5.0f1: обход известного бага ShaderGraph 17.4 (CS0246 GUID) из 6000.4 —
  # исправлен только в 6000.5.0a3+. URP/ShaderGraph закреплены на 17.5.0.
  UNITY_VERSION: 6000.5.0f1

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-test-${{ runner.os }}-${{ env.UNITY_VERSION }}-${{ hashFiles('Packages/manifest.json', 'Packages/packages-lock.json') }}
          restore-keys: |
            Library-test-${{ runner.os }}-${{ env.UNITY_VERSION }}-

      - name: Run Unity Tests
        uses: game-ci/unity-test-runner@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          unityVersion: ${{ env.UNITY_VERSION }}
          testMode: EditMode
          checkName: Test Results
          coverageOptions: 'generateAdditionalMetrics;generateHtmlReport;generateBadgeReport'

  build:
    name: Build
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Library and Packages
        uses: actions/cache@v4
        with:
          path: |
            Library
            Packages
          key: Library-build-${{ runner.os }}-${{ env.UNITY_VERSION }}-${{ hashFiles('Packages/manifest.json', 'Packages/packages-lock.json') }}
          restore-keys: |
            Library-build-${{ runner.os }}-${{ env.UNITY_VERSION }}-
            Library-test-${{ runner.os }}-${{ env.UNITY_VERSION }}-

      - name: Build Unity Project
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: StandaloneWindows64
          unityVersion: ${{ env.UNITY_VERSION }}
          buildMethod: BuildValidator.ForceBuild
          versioning: Semantic
          allowDirtyBuild: true
          customParameters: -logFile - -buildTarget StandaloneWindows64
          # Оптимизация: кеширование библиотек Unity между сборками
          # Документация: https://game.ci/docs/github/builder

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: WhitecoatPerpetual_Win64
          path: build/StandaloneWindows64/
          retention-days: 7

      - name: Upload Logs on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: BuildLogs
          path: |
            /github/workspace/Logs/
            /github/workspace/BuildLogs/
          retention-days: 3

  release:
    name: Release Windows
    needs: build
    # Публикуем GitHub Release только когда коммит помечен [release]
    if: ${{ github.event_name == 'push' && contains(github.event.head_commit.message, '[release]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: WhitecoatPerpetual_Win64
          path: build/StandaloneWindows64/

      - name: Zip Build (portable exe)
        run: |
          cd build/StandaloneWindows64
          zip -qr "${{ github.workspace }}/WhitecoatPerpetual_Win64.zip" .
          cd "${{ github.workspace }}"
          ls -la WhitecoatPerpetual_Win64.zip

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="v0.2.0-win64"
          TITLE="Белёк: Вечный Прибой — Windows 64-bit"
          NOTES="Играбельная Windows-сборка (64-bit).

          **Запуск:** распакуйте архив и запустите \`WhitecoatPerpetual.exe\`.

          Управление: WASD — плавание, Shift — ускорение, Space — всплытие/прыжок,
          Ctrl — нырок, E — взаимодействие.

          Сборка: Unity 6000.5.0f1 (URP 17.5), IL2CPP, StandaloneWindows64.
          Коммит: ${{ github.sha }}"
          gh release create "$TAG" --title "$TITLE" --notes "$NOTES" --clobber || true
          gh release upload "$TAG" "WhitecoatPerpetual_Win64.zip" --clobber
          echo "Release ready: https://github.com/${{ github.repository }}/releases/tag/$TAG"
